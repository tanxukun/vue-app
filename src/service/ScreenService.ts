

export default class ScreenService {
    static pushPeers: Map<string, RTCPeerConnection> = new Map()
    static remotePeers: Map<string, RTCPeerConnection> = new Map()

    static openScreen () {
        
    }
}